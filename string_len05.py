def main(s1,s2):
    """
    Given two strings, s1 and s2. Find their total length.
    Args:
        s1: string
        s2: string
    Returns:
        total length of strings
    """
    s3 = len(s1) + len(s2)
    return s3
print(main('code','python'))